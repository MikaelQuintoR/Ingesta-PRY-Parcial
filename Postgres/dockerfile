FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir psycopg2-binary
COPY data/ ./data/
COPY ingestpostgres.py .

# Exponer puerto del contenedor (reemplazar antes de usar)
CMD ["python3", "ingestpostgres.py"]